{
  "mcpServers": {
    "training-app": {
      "type": "http",
      "url": "${TRAINING_APP_MCP_URL:-https://training-app-production-e048.up.railway.app}/mcp/",
      "headers": {
        "Authorization": "Bearer ${TRAINING_APP_MCP_TOKEN}"
      }
    }
  }
}
